#!/usr/bin/env bash
set -euo pipefail
source_path=$(python3 -c 'import os, sys; print(os.path.realpath(sys.argv[1]))' "${BASH_SOURCE[0]}")
export AGENT_SESSION=1
exec "$(dirname "$source_path")/run.sh" "$@"
